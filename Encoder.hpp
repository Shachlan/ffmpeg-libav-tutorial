#include <string>
using std::string;

#include "Buffers.hpp"

class TranscodingComponents;

// class Encoder {
//     public:
//     Encoder(string file_name, string video_codec_name, int width, int height, double framerate);
//     ~Encoder();
//     void encode_video_frame(VideoFrame frame);
//     private:
//     TranscodingComponents *video_encoder;
//     TranscodingComponents *audio_encoder;
// };